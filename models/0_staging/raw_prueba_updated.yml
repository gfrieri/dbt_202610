version: 2

sources:
  - name: raw
    tables:
      - name: raw_customers
      - name: raw_orders

models:
  - name: stg_orders
    columns:
      - name: id_pedido
        tests:
          - not_null
          - unique
          - no_negative_values:
              column_name: id_pedido